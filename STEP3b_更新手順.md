# 📊 STEP 3b 更新手順 ― ダッシュボードに実データを表示

トークンは設定済み（`THREADS_ACCESS_TOKEN`）。あとは新しいファイルをGitHubに上げるだけ。

## 変わった/増えたファイル
- `index.html`（ダッシュボード本体・上書き）
- `api/insights.js`（データ取得の裏方・新規）
- `vercel.json`（時間制限をのばす設定・新規）
- ※ `api/auth.js` `api/callback.js` は今回使わないが、置いたままでOK

## やること
1. GitHubの `threads-nyanseki` リポジトリを開く
2. **「Add file → Upload files」**
3. パソコンから、**`index.html` / `vercel.json` / `api` フォルダ**をドラッグ＆ドロップ
   - `index.html` は上書き、`api/insights.js` と `vercel.json` は追加される
4. **「Commit changes」**
5. Vercelが自動で再デプロイ（1〜2分）
6. 本番URL（`https://（あなたのURL）`）を開く
   → 少し待つと、あなたのThreadsの分析が表示されます🎉

## 表示されるもの
- 期間切替（直近1週間 / 30日 / 全期間）
- サマリー（平均表示・いいね・エンゲージ・EG率＋世間の目安）
- 時間帯別（平均いいね・平均表示）
- 週ごとの推移
- 反応の内訳＋あなたのタイプ
- 投稿タイプ別（文字 / 画像）
- 伸びるキーワード
- ウケた投稿ランキング（自己平均比）
- 猫からのひとことアドバイス

## うまくいかない時
- 画面に「😿 データを取得できませんでした」＋赤い文字が出たら、**その赤い文字をそのまま教えて**ください（原因を特定します）。
  - よくある原因：トークンの権限不足／期限切れ／アカウントが非公開
- ずっと「分析しています…」のままの時は、投稿数が多くて時間切れかも → 教えてください（取得量を調整します）

## 次（STEP 2）
- 全期間の高速表示（結果を保存＝キャッシュ）
- フォロワー数の毎日自動記録＋成長の推移グラフ🌱
- トークンの自動更新（60日ごと）
