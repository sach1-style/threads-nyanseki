# 🔑 Threadsにゃんせき ― トークン準備ガイド

自分のThreadsアカウントの分析データ（表示・いいね等）を取るための準備メモ。

## 🎉 前提：個人利用は「審査なし」でOK
- 分析権限 `threads_manage_insights` は本来アプリ審査が必要だが、
  **自分＝テスター(開発者)として自分のアカウントを見るぶんは審査不要**。
- だから、自分のThreadsアカウントを「Threadsテスター」に追加して使う。

## 🔒 いちばん大事な注意
- **アプリの「シークレット(App Secret)」と「アクセストークン」は、絶対にチャットに貼らない！**
  → これらは Vercel の「環境変数(＝秘密の金庫)」に、あなた自身が入れる。私は中身を見ません。
- 画面側のコードにトークンを書かない（裏側だけで扱う）。

---

## 🚶 進める順番（チキン&エッグ回避）

### STEP 1【あなた】Metaアプリを作る
1. https://developers.facebook.com/ にログイン
2. マイアプリ → 「アプリを作成」
3. ユースケースで **「Threads API を利用」** を選ぶ
4. アプリ名（例：threads-nyanseki）を付けて作成

### STEP 2【あなた】権限とテスターを設定
5. アプリ内「ユースケース → Threads → カスタマイズ」で、権限を追加：
   - `threads_basic`（基本情報の取得）
   - `threads_manage_insights`（分析データの取得）
   ※ 投稿はしないので publish 系は不要
6. 「アプリの役割 / テスター」に、**自分のThreadsアカウントをThreadsテスターとして追加**
   → Threadsアプリ側（設定＞アカウント＞ウェブサイトのアクセス許可 等）で招待を承認
7. 「Threads → 設定」にある **Threads App ID** と **Threads App Secret** を控える
   （App Secret は秘密！メモは自分だけの場所に）

### STEP 3【私】アプリの骨組みを作って公開
- 「Threadsでログイン」ボタン＋裏側(トークンを受け取る窓口)を作る
- Vercelに公開して、アプリのURL（例：https://threads-nyanseki.vercel.app）を確定

### STEP 4【あなた】URLを登録＆秘密をしまう
- Metaアプリの **「リダイレクトURI」** に、STEP3で決まった
  `https://（アプリのURL）/api/callback` を登録
- Vercelの「環境変数」に、App ID・App Secret を登録（あなたが入力）

### STEP 5【あなた→自動】ログインしてトークン取得
- 公開したアプリで **「Threadsでログイン」** を押して許可
- 裏側が自動で「長期トークン(60日)」を取得・保管
- 以降、アプリが自動で分析データを取得！

### STEP 6【私】60日ごとの自動更新
- トークンが切れる前に自動で更新する仕組みを入れる（あなたの手間なし）

---

## 📎 まず着手するのは STEP 1〜2（あなたのMeta側の設定）
STEP 3以降は私がアプリを作ってから、URLなどをお渡しして一緒に進めます。
分からない画面が出たら、スクショを送ってくれればOK🐱

## 参考リンク
- 初心者向け解説（日本語・画像つき）: https://note.com/youpapalife/n/ne22b0e34ad1a
- 権限と審査について: https://singhamandeep.com/threads-api-app-review-permissions/
